import { createStyles } from "@material-ui/core/styles";

export interface Properties {
    card: object;
}

const messageStyles: Properties = {
    card: createStyles({
        root: {

        },
    }),
};

export default messageStyles;
